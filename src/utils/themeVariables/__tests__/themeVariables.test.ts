import { themeVariables, themeStyles } from "../themeVariables";

import { describe, it, expect } from "vitest";

describe("themeVariables.ts", () => {
  it("should return the correct CSS variable for each theme variable", () => {});
});
